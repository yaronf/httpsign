name: Document Go Code

on:
  push:
    branches:
      - master

jobs:
  godoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.17'

      - name: godoc
        run: |
         mkdir docs
         cd docs
         ../gendoc.sh 
